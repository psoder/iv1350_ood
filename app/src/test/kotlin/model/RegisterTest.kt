package model

import kotlin.test.*

class RegisterTest {

    @Test fun `paying increases balance`() {}
    
    @Test fun `newTransactions fails if there is current transaction`() {}

    @Test fun `ending transaction set transactioin to null`() {}
    
    @Test fun `enter item updates Transaction`() {}
    
    @Test fun `pay fails if no transaction`() {}
    
    @Test fun `enterItem fails if no transaction`() {}

    @Test fun `applyDiscount fails if no transaction`() {}    

}